export enum tagTypes {
  admin = "admin",
  user = "user",
  request = "request",
  post = "post",
  comment = "comment",
  notification = "notification",
}

export const tagTypesList = [tagTypes.admin, tagTypes.user, tagTypes.request];
